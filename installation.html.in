#define TITLE Swine - Installation
#define SECTION_INSTALLATION
#include <header.html>

LEFT_SIDE

<h3>Downloads</h3>
<ul>
<li>Open downloads page</li>
<li>Download tags</li>
<li>Most current version [tar.gz] [zip]</li>
</ul>

CONTENT

<h3>Installing swine on debian-based systems</h3>

<pre><code>$ sudo apt-add-repository 'deb http://dswd.github.com/Swine/repository/deb stable main'
$ sudo apt-get update
$ sudo apt-get install swine
</code></pre>

#include <footer.html>
